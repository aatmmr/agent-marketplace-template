{
  "name": "agentic-marketplace-website",
  "private": true,
  "version": "1.0.0",
  "description": "Agentic Marketplace: searchable catalog of plugins, skills, agents and MCP files.",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-manifest.js",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.1",
    "dompurify": "^3.1.6",
    "fuse.js": "^7.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^13.0.0"
  }
}
